---
clusterapi_install_become: false
clusterapi_install_infrastructure: openstack
clusterapi_install_kubeconfig: kubeconfig
clusterapi_install_cluster_topology: true
clusterapi_install_uid: "{{ ansible_facts.user_uid }}"
clusterapi_install_gid: "{{ ansible_facts.user_gid }}"
