---
- name: Initialize Cluster API
  become: "{{ clusterapi_install_become }}"
  ansible.builtin.command: /usr/local/bin/clusterctl --kubeconfig {{ clusterapi_install_kubeconfig }} init --infrastructure {{ clusterapi_install_infrastructure }}
  register: result
  changed_when: "'Your management cluster has been initialized successfully' in result.stdout"
